/**
 * 이 패키지는 mock 구현을 포함합니다.
 * 스프링 3.1에서 소개된 {@link org.springframework.core.env.Environment Environment} 와
 * {@link org.springframework.core.env.PropertySource PropertySource}
 * 를 mock으로 감싸서 테스트 할 수 있습니다.
 *
 * 이 mock 구현체들은 컨테이너 밖에서 environment 프로퍼티에 의존하는 유닛 테스트를  작성하는데 유용합니다.
 */

package org.springframework.mock.env;

